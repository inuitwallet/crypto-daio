wrapper of daemon

block notify sends block hash to local endpoint
    block hash stored and block requested from daemon
    block parsed and data stored in database (do this for all blocks historically (create block explorer)

request new account.
    HD wallet created and extended public key shown to user.
    User can create any number of addresses tied to the same private key

watch address
    ask for notification of a transaction to an address
    send notification to an endpoint specified in the request
    Time limit on watch?


